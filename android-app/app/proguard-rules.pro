# Keep rules should be added only when a release shrink test proves they are needed.
# Room generated implementations and kotlinx.serialization serializers are referenced
# directly by generated code and do not require broad package-level keeps.
